# 진행 상황 & 다음 작업

마지막 업데이트: 2026-08-03
라이브: https://pokemon.wmf34a.workers.dev
저장소: https://github.com/wmf34a/pokemon

## 지금까지 한 일

### 1. 기획 & 데이터
- 기획안(`pokemon_game_plan.docx` — Claude Desktop 세션 산출물, 이 저장소 밖에 있음)을 참고해 개발
- PokeAPI에서 전체 포켓몬(1025마리) 데이터를 미리 받아 `public/data/pokemon.json`에 캐싱 (`scripts/fetch-pokemon-data.mjs`)
- 울음소리는 PokeAPI `cries` 필드(게임 인게임 효과음) 사용 — **애니메이션 성우 목소리("피카~츄~")가 아님을 확인**. 유튜브에서 애니 음성을 추출해 쓰는 방안은 저작권 문제로 보류하고, 게임 효과음을 계속 쓰기로 결정함.
- 진화 체인 데이터(`evolvesFrom`/`evolvesTo`/`evolutionStage`)를 fetch 스크립트에서 추가 수집해 `pokemon.json`에 포함시킴

### 2. 화면 구현
- 홈 (`Home.jsx`): "내 포켓몬" 현황 섹션(진행 바, 진화 셀레브레이션, 분기 진화 선택 UI) + 퀴즈 바로가기
- 도감 (`Dex.jsx`): 전체 목록, 가나다순/도감번호순/세대순 정렬, 초성 검색, 타입 필터 칩
- 포켓몬 상세 (`PokemonDetail.jsx`): 이미지, 타입, 키/몸무게, 특성, 설명, 울음소리 재생, **진화 계보 표시**(선형/분기 모두 지원, 현재 보고 있는 포켓몬 강조, 없으면 섹션 자체를 숨김)
- 퀴즈 허브 (`QuizHub.jsx`): 모드 선택 + "1세대(#1~#151) 포켓몬만 출제" 토글 (localStorage 저장)
- **내 포켓몬** (`ChooseStarter.jsx` / `MyPokemon.jsx`): 스타터 선택, 포인트 적립에 따른 자동/분기 진화, 최종 진화 시 졸업, 언제든 "다른 포켓몬 고르기"로 리셋
- 퀴즈 5종 구현 완료 (모두 힌트 단계별 공개 + 점수, 세션당 20문제 제한 + 결과 요약 화면):
  - 실루엣 퀴즈 (`SilhouetteQuiz.jsx`)
  - 울음소리 퀴즈 (`CryQuiz.jsx`)
  - 진화 순서 맞추기 (`EvolutionQuiz.jsx`) — 진화 전후 순서 배열
  - 타입 퀴즈 (`TypeQuiz.jsx`) — 제시된 타입(단일/복합)을 가진 포켓몬을 4마리 중 고르기
  - 누구냔 넌 (`ZoomQuiz.jsx`) — 아트워크를 확 확대해서 일부만 보여주고, 힌트를 누를 때마다 줌아웃
  - ~~초성 퀴즈~~ 삭제됨 (실루엣/울음소리 퀴즈가 이미 초성 힌트를 포함하고 있어 중복 판단)

### 3. 내 포켓몬 & 진화 시스템 (2026-08-02~03)
- 퀴즈 정답 시 포인트 적립 → 누적 200점마다 진화(단일 경로는 자동, 분기 경로는 홈에서 직접 선택)
- 진화가 일어나면 **퀴즈 화면에도 작은 토스트**(스파클 + 새 단계 스프라이트 + 합성 축하음)가 즉시 뜨고, 실제 리빌 연출은 홈 화면에서 별도로 재생 — 토스트는 예고편, 홈이 본편
- 사운드는 외부 에셋 없이 Web Audio API로 합성(3음 상승 아르페지오)
- 최종 진화에 도달하면 "졸업" 처리, 언제든 리셋 가능(누적 포인트/도감 컬렉션은 유지)

### 4. 퀴즈 세션 제한 (2026-08-03)
- 기존에는 한 번 들어가면 무한 라운드였음 → 세션당 20문제로 제한(`SESSION_LENGTH`, `src/utils/pokemonData.js`)
- 20문제를 다 풀면 결과 요약("N문제 중 M문제 정답 · 총 X점") + "다시 하기"(새 세션)/"퀴즈 목록으로" 버튼
- 재진입/재시작마다 순수 랜덤으로 다시 뽑음(직전 세션과 중복 회피는 하지 않음 — 의도된 설계)
- 결과 화면은 5개 퀴즈가 공통으로 쓰는 `QuizResultScreen` 컴포넌트로 통합

### 5. 버그 수정 이력
- **힌트 버튼 오탭**: 힌트가 누적되며 "힌트 더보기" 버튼이 아래로 밀려, 연타 시 다른 요소를 잘못 누르는 문제 → 버튼을 힌트 텍스트보다 먼저(고정 위치) 배치하도록 각 퀴즈 모두 수정
- **울음소리 불일치**: `<audio src>`만 갱신하고 엘리먼트를 재사용해 이전 라운드 소리가 남아있던 문제 → `key={answer.id}`로 라운드마다 완전히 새로 mount 되도록 수정
- **package-lock.json 불일치로 CI 실패**: 완전히 새로 생성해서 해결, Node 20/22/24 교차 검증
- Cloudflare 쪽에서 자동으로 Workers 설정 PR을 올려서 (Pages가 아니라 Workers Static Assets 방식) merge 후 관련 문서/Makefile 전부 Workers 기준으로 재작성

### 6. 개발 환경/배포 인프라
- Lint: oxlint (`.oxlintrc.json`, react 플러그인)
- Test: Vitest + Testing Library (`src/**/*.test.{js,jsx}`, 87개 테스트, 전부 통과)
- Git hooks: husky + lint-staged (pre-commit: lint-staged, pre-push: 전체 테스트)
- CI: `.github/workflows/ci.yml` (push/PR 시 lint → test → build)
- 배포: Cloudflare Workers (Static Assets), `wrangler.jsonc` 설정, GitHub 연동으로 push마다 자동 배포 + `npm run deploy`로 수동 즉시 배포도 가능
- 명령어 모음: `Makefile` (`make install/dev/lint/test/build/ci/deploy`)
- 자세한 배포 절차는 `Cloudflare_Workers_배포_가이드.md` 참고
- 이번 작업들은 `docs/superpowers/specs/`(디자인 스펙)와 `docs/superpowers/plans/`(구현 계획) 아래 날짜별로 문서화되어 있음

## 다음 작업 (남은 것)

기획안에 있었지만 아직 다루지 않은 것:
- 스무고개(예/아니오 힌트), 카드 짝맞추기, 오늘의 포켓몬(일일 챌린지), 스피드런&랭킹
- 연속 정답 스트릭 등 추가 localStorage 기반 기록
- 도감(Dex) 페이지에 세대 필터 칩 추가 (현재 퀴즈에만 "1세대만" 토글이 있고, 도감 목록 자체에는 세대 필터가 없음)

~~타입 상성 퀴즈~~ / ~~도감번호 업다운~~ (숫자야구 스타일) — QuizHub의 미구현 placeholder였으나, 전자는 실제로 구현된 "타입 퀴즈"(제시된 타입 가진 포켓몬 고르기)로 컨셉이 바뀌어 대체되었고, 후자는 placeholder 자체를 삭제함.

## 다시 시작할 때 체크리스트
- [ ] `npm install` (또는 `make install`)
- [ ] `make dev`로 로컬 확인, 또는 https://pokemon.wmf34a.workers.dev 에서 현재 배포본 확인
- [ ] 위 "다음 작업" 중 우선순위 정해서 진행
- [ ] 완료 후 `make ci` → 커밋/push → CI 통과 확인 → `make deploy`(또는 `npm run deploy`)
