# 진행 상황 & 다음 작업

마지막 업데이트: 2026-07-21
라이브: https://pokemon.wmf34a.workers.dev
저장소: https://github.com/wmf34a/pokemon

## 지금까지 한 일

### 1. 기획 & 데이터
- 기획안(`pokemon_game_plan.docx` — Claude Desktop 세션 산출물, 이 저장소 밖에 있음)을 참고해 개발
- PokeAPI에서 전체 포켓몬(1025마리) 데이터를 미리 받아 `public/data/pokemon.json`에 캐싱 (`scripts/fetch-pokemon-data.mjs`)
- 울음소리는 PokeAPI `cries` 필드(게임 인게임 효과음) 사용 — **애니메이션 성우 목소리("피카~츄~")가 아님을 확인**. 유튜브에서 애니 음성을 추출해 쓰는 방안은 저작권 문제로 보류하고, 게임 효과음을 계속 쓰기로 결정함.

### 2. 화면 구현
- 홈 (`Home.jsx`): "포켓몬 도감" / "퀴즈 바로가기"(→ `/quiz` 퀴즈 허브) 두 카드
- 도감 (`Dex.jsx`): 전체 목록, 가나다순/도감번호순/세대순 정렬, 초성 검색, 타입 필터 칩
- 포켓몬 상세 (`PokemonDetail.jsx`): 이미지, 타입, 키/몸무게, 특성, 설명, 울음소리 재생
- 퀴즈 허브 (`QuizHub.jsx`): 모드 선택 + "1세대(#1~#151) 포켓몬만 출제" 토글 (localStorage 저장, 큰 스위치 카드 UI)
- 퀴즈 3종 구현 완료 (모두 힌트 단계별 공개 + 객관식/주관식 + 점수):
  - 실루엣 퀴즈 (`SilhouetteQuiz.jsx`)
  - 울음소리 퀴즈 (`CryQuiz.jsx`)
  - 초성 퀴즈 (`ChosungQuiz.jsx`)

### 3. 버그 수정 이력
- **힌트 버튼 오탭**: 힌트가 누적되며 "힌트 더보기" 버튼이 아래로 밀려, 연타 시 다른 요소를 잘못 누르는 문제 → 버튼을 힌트 텍스트보다 먼저(고정 위치) 배치하도록 3개 퀴즈 모두 수정
- **울음소리 불일치**: `<audio src>`만 갱신하고 엘리먼트를 재사용해 이전 라운드 소리가 남아있던 문제 → `key={answer.id}`로 라운드마다 완전히 새로 mount 되도록 수정 (CryQuiz/ChosungQuiz/SilhouetteQuiz/PokemonDetail 전체 적용)
- **package-lock.json 불일치로 CI 실패**: 완전히 새로 생성해서 해결, Node 20/22/24 교차 검증
- Cloudflare 쪽에서 자동으로 Workers 설정 PR을 올려서 (Pages가 아니라 Workers Static Assets 방식) merge 후 관련 문서/Makefile 전부 Workers 기준으로 재작성

### 4. 개발 환경/배포 인프라
- Lint: oxlint (`.oxlintrc.json`, react 플러그인)
- Test: Vitest + Testing Library (`src/**/*.test.{js,jsx}`, 15개 테스트)
- Git hooks: husky + lint-staged (pre-commit: lint-staged, pre-push: 전체 테스트)
- CI: `.github/workflows/ci.yml` (push/PR 시 lint → test → build)
- 배포: Cloudflare Workers (Static Assets), `wrangler.jsonc` 설정, GitHub 연동으로 push마다 자동 배포 + `make deploy`로 수동 즉시 배포도 가능
- 명령어 모음: `Makefile` (`make install/dev/lint/test/build/ci/deploy`)
- 자세한 배포 절차는 `Cloudflare_Workers_배포_가이드.md` 참고

## 다음 작업 (남은 것)

기획안 기준으로 QuizHub에 자리만 잡혀있고 미구현인 것들 (`src/pages/QuizHub.jsx`의 `ready: false` 항목):

1. **타입 상성 퀴즈** ("이 포켓몬에게 효과가 굉장한 타입은?") — 타입 상성표 데이터 필요
2. **진화 순서 맞추기** — 진화 전/후 이미지 순서 배열 (evolution_chain 데이터 활용 필요, 현재 `pokemon.json`에 `evolutionChainUrl`만 있고 실제 체인 데이터는 미수집)
3. **도감번호 업다운** (숫자야구 스타일)

그 외 기획안에 있었지만 아직 다루지 않은 것:
- 스무고개(예/아니오 힌트), 카드 짝맞추기, 오늘의 포켓몬(일일 챌린지), 스피드런&랭킹
- 컬렉션/도감 채우기, 연속 정답 스트릭 등 localStorage 기반 기록 저장 (README에도 미구현으로 표시됨)
- 도감(Dex) 페이지에 세대 필터 칩 추가 (현재 퀴즈에만 "1세대만" 토글이 있고, 도감 목록 자체에는 세대 필터가 없음)

## 다시 시작할 때 체크리스트
- [ ] `npm install` (또는 `make install`)
- [ ] `make dev`로 로컬 확인, 또는 https://pokemon.wmf34a.workers.dev 에서 현재 배포본 확인
- [ ] 위 "다음 작업" 중 우선순위 정해서 진행
- [ ] 완료 후 `make ci` → 커밋/push → CI 통과 확인 → `make deploy`
